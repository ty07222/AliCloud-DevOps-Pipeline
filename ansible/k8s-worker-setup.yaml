---
- name: 配置 Kubernetes Worker 节点
  hosts: k8s_workers
  become: yes
  vars:
    master_host: "master1"  # 使用 inventory.ini 中的主节点主机名
    join_command_file: "./kubeadm-join-commands/{{ master_host }}-join-command.sh"  # 指向控制机上保存的 join 命令文件
  tasks:
    - name: 设置节点主机名
      ansible.builtin.hostname:
        name: "{{ inventory_hostname }}"
      # 注释：将节点主机名设置为 Ansible 库存中的主机名

    - name: 添加 Docker CE 和 Kubernetes 仓库
      ansible.builtin.shell:
        cmd: |
          yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
          cat <<EOF > /etc/yum.repos.d/kubernetes.repo
          [kubernetes]
          name=Kubernetes
          baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
          enabled=1
          gpgcheck=1
          repo_gpgcheck=1
          gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg
          EOF
      changed_when: true
      # 注释：配置软件源

    - name: 禁用 swap 并注释 fstab 中的 swap 行
      ansible.builtin.shell:
        cmd: |
          swapoff -a
          sed -i '/\sswap\s/s/^/#/' /etc/fstab
      changed_when: true
      # 注释：禁用 swap 以满足 Kubernetes 要求

    - name: 配置内核模块和 sysctl 设置
      ansible.builtin.copy:
        dest: "{{ item.dest }}"
        content: "{{ item.content }}"
        owner: root
        group: root
        mode: '0644'
      loop:
        - dest: /etc/modules-load.d/k8s.conf
          content: |
            overlay
            br_netfilter
        - dest: /etc/sysctl.d/k8s.conf
          content: |
            net.bridge.bridge-nf-call-iptables  = 1
            net.bridge.bridge-nf-call-ip6tables = 1
            net.ipv4.ip_forward                 = 1
      # 注释：持久化内核模块和网络设置

    - name: 加载内核模块并应用 sysctl 设置
      ansible.builtin.shell:
        cmd: |
          modprobe overlay
          modprobe br_netfilter
          sysctl --system
      changed_when: false
      # 注释：确保模块加载和 sysctl 参数生效

    - name: 安装必要软件包并更新缓存
      ansible.builtin.dnf:
        name:
          - yum-utils
          - device-mapper-persistent-data
          - lvm2
          - containerd.io
          - kubelet
          - kubeadm
          - kubectl
        state: present
        update_cache: yes
        use_backend: dnf4
      # 注释：安装依赖和 Kubernetes 组件

    - name: 配置 containerd
      ansible.builtin.shell:
        cmd: |
          mkdir -p /etc/containerd
          containerd config default > /etc/containerd/config.toml
          sed -i 's/SystemdCgroup = false/SystemdCgroup = true/g' /etc/containerd/config.toml
      changed_when: true
      notify: 重启 containerd
      # 注释：生成 containerd 配置并启用 SystemdCgroup

    - name: 确保 containerd 运行
      ansible.builtin.systemd:
        name: containerd
        state: started
        enabled: yes
      retries: 3
      delay: 5
      register: containerd_start
      until: containerd_start.status.ActiveState == "active"
      # 注释：确保 containerd 服务启动并运行

    - name: 等待 containerd 套接字
      ansible.builtin.wait_for:
        path: /var/run/containerd/containerd.sock
        state: present
        timeout: 30
      retries: 3
      delay: 5
      # 注释：确保 containerd 套接字可用，防止拉取镜像失败

    - name: 预拉取 Kubernetes 镜像
      ansible.builtin.command:
        cmd: kubeadm config images pull --image-repository=registry.aliyuncs.com/google_containers --cri-socket=unix:///var/run/containerd/containerd.sock
      changed_when: true
      # 注释：拉取 Kubernetes 所需镜像

    - name: 从控制机读取 join 命令文件
      ansible.builtin.slurp:
        src: "{{ join_command_file }}"
      register: join_file_content
      delegate_to: localhost
      # 注释：读取保存在控制机上的 join 命令文件

    - name: 提取 join 命令
      ansible.builtin.set_fact:
        kubeadm_join_command: "{{ join_file_content['content'] | b64decode | regex_findall('kubeadm join .+') | first | trim }}"
      when: join_file_content['content'] is defined
      # 注释：从文件中提取 kubeadm join 命令（假设文件包含完整的命令）

    - name: 执行 kubeadm join 加入集群
      ansible.builtin.command:
        cmd: "{{ kubeadm_join_command }} --cri-socket=unix:///var/run/containerd/containerd.sock"
      register: kubeadm_join_result
      retries: 3
      delay: 10
      until: kubeadm_join_result.rc == 0
      changed_when: true
      when: kubeadm_join_command is defined
      # 注释：使用提取的 join 命令加入集群

    - name: 在 worker 节点创建 .kube 目录
      ansible.builtin.file:
        path: ~/.kube
        state: directory
        mode: '0755'
      # 注释：创建 ~/.kube 目录

    - name: 从控制机复制 kubeconfig 到 worker 节点
      ansible.builtin.copy:
        src: "./kubeadm-join-commands/{{ master_host }}-kubeconfig"
        dest: ~/.kube/config
        owner: root
        group: root
        mode: '0644'
      # 注释：从控制机的 kubeadm-join-commands 目录复制 kubeconfig 到 worker 节点

    - name: 设置 kubeconfig 权限
      ansible.builtin.shell:
        cmd: chown $(id -u):$(id -g) ~/.kube/config
      changed_when: true
      # 注释：设置 kubeconfig 权限

    - name: 检查集群节点状态
      ansible.builtin.shell:
        cmd: kubectl get nodes
        executable: /bin/bash
      environment:
        KUBECONFIG: "{{ ansible_env.HOME }}/.kube/config"
      register: node_status
      changed_when: false
      # 注释：验证集群节点状态

    - name: 显示集群状态
      ansible.builtin.debug:
        msg: "集群状态：\n{{ node_status.stdout_lines | default(['无输出']) }}"
      when: node_status.stdout is defined
      # 注释：输出集群状态

  handlers:
    - name: 重启 containerd
      ansible.builtin.systemd:
        name: containerd
        state: restarted
        enabled: yes
      # 注释：containerd 配置更改后重启服务
